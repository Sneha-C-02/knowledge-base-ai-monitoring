version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://backend:3000/api
    # Note: If running alongside the backend docker-compose, make sure they share a network.
