version: '3.8'
services:
  api:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    command: uvicorn src.knowledge_base_backend.main:app --host 0.0.0.0 --port 3000 --reload

